dsl_version: ctrl.v1
id: CTL.S3.PUBLIC.008
name: No Public List via Policy
description: >
  S3 bucket policies must not grant anonymous object listing.
domain: exposure
exposure:
  type: public_list_via_policy
  principal_scope: public
severity: critical
scope_tags:
  - aws
  - s3
type: unsafe_state
params: {}
remediation:
  description: >
    Bucket policy grants public listing (Principal \"*\" with list action).
  action: >
    Remove or constrain policy statements allowing s3:ListBucket to
    anonymous principals.
  example: |
    {
      "storage": {
        "visibility": {
          "public_list_via_policy": false
        }
      }
    }
unsafe_predicate:
  any:
    - field: properties.storage.visibility.public_list_via_policy
      op: eq
      value: true
