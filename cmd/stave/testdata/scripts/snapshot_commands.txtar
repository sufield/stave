# Verify the snapshot group exposes only `diff`. The lifecycle
# subcommands (quality, plan, upcoming, status, risk) were removed —
# snapshot lifecycle management is the upstream system's job.

exec stave snapshot --help
stdout 'diff'
! stdout 'upcoming'
! stdout 'quality'
! stdout 'prune'

exec stave snapshot diff --help
stdout 'Exit Codes'
