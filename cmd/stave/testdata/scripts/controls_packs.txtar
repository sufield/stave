# Verify controls and packs commands

# Controls list built-in
exec stave controls list --built-in
stdout 'CTL.S3'

# Controls aliases
exec stave controls aliases
stdout 's3.'

# Packs list
exec stave pack list
stdout 'entropy'

# Controls help
exec stave controls --help
stdout 'list'
stdout 'explain'
stdout 'aliases'

# Packs help
exec stave pack --help
stdout 'list'
stdout 'show'
