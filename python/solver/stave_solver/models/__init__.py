"""Service-specific Z3 models.

Iter 3.1.2 adds ``s3.py`` for bucket public-exposure modeling.
Future iterations add per-service modules (``iam.py``,
``kms.py``, ...). The empty package today is intentional —
3.1.1 is the harness-only scaffold.
"""
