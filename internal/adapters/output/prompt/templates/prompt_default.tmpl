# Stave Finding Analysis

I am using **Stave**, an offline configuration safety evaluator, to detect infrastructure misconfigurations. Stave found **{{ .FindingCount }} finding(s)** for asset `{{ .AssetID }}` that I need help analyzing and correcting.
{{range .Findings}}
---

## Finding: {{ .ControlID }}

| Field | Value |
|-------|-------|
| Control | {{ .ControlID }} |
| Name | {{ .ControlName }} |
| Description | {{ .Description }} |
| Asset | {{ .AssetID }} |
| Asset Type | {{ .AssetType }} |

### Evidence

{{ .Evidence }}
{{if .MatchedProps}}
### Misconfigurations

{{ .MatchedProps }}
{{end}}{{if .RootCauses}}
### Root Causes

{{ .RootCauses }}
{{end}}{{if .ControlYAML}}
### Control Definition (YAML)

```yaml
{{ .ControlYAML }}
```
{{end}}{{if .Guidance}}
### Control Guidance

{{ .Guidance }}
{{end}}{{end}}{{if .AssetProperties}}
## Asset Properties (Latest Snapshot)

```json
{{ .AssetProperties }}
```
{{end}}
## What I Need

Based on the findings above, please provide:

1. **Root cause analysis** — Why is this asset in an unsafe state?
2. **Corrective changes** — Specific, actionable changes to address each finding.
3. **Verification** — How to confirm the fix is applied correctly.
4. **Prevention** — What controls or automation would prevent recurrence?
