dsl_version: ctrl.v1
id: CTL.AZURE.MYSQL.SSL.001
name: MySQL SSL Enforcement Not Enabled
description: >
  SSL enforcement disabled. Connections to the database can be
  unencrypted — credentials and query data transmitted in plaintext.
domain: encryption
severity: high
compliance:
  cis_azure_v2: "4.4.1"
  nist_800_53_r5: "SC-8"
  hipaa: "164.312(e)(1)"
  pci_dss_v4: "4.1"
  soc2: "CC6.7"
scope_tags: [azure]
taxonomy:
  - compliance-mapping
  - credential-lifecycle
  - encryption-in-transit
type: unsafe_state
classification: state_assertion
params:
  attack_stage: initial_access
remediation:
  description: MySQL SSL Enforcement Not Enabled — finding detected.
  action: Enable SSL enforcement on the MySQL server.
  example: |
    {"azure": {"database": {"kind": "mysql_server", "mysql": {"ssl_enforcement": true}}}}
unsafe_predicate:
  all:
    - field: properties.azure.database.kind
      op: eq
      value: mysql_server
    - field: properties.azure.database.mysql.ssl_enforcement
      op: eq
      value: false
