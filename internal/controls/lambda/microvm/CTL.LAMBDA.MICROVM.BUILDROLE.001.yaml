dsl_version: ctrl.v1
id: CTL.LAMBDA.MICROVM.BUILDROLE.001
name: MicroVM Build Role Is Over-Privileged
description: >
  MicroVM Build Role Is Over-Privileged. Lab control for the Lambda MicroVM validation suite
  (ctf/labs/microvm). Evaluates a captured MicroVM configuration snapshot
  (obs.v0.1) for this invariant.
domain: identity
severity: high
compliance:
  nist_800_53_r5: "AC-6"
scope_tags:
  - aws
  - lambda
  - microvm
taxonomy:
  - compliance-mapping
  - compute-hardening
type: unsafe_state
classification: state_assertion
applicable_asset_types:
  - aws_iam_role
params:
  attack_stage: persistence
remediation:
  description: >
    MicroVM Build Role Is Over-Privileged — finding detected on the captured MicroVM configuration.
  action: >
    Scope the build role to S3 read of the artifact bucket, ECR push, and CloudWatch logs only.
  example: |
    {
      "role": {
        "kind": "iam_role",
        "purpose": "build",
        "over_privileged": false
      }
    }
unsafe_predicate:
  all:
    - field: properties.role.kind
      op: eq
      value: iam_role
    - field: properties.role.purpose
      op: eq
      value: build
    - field: properties.role.over_privileged
      op: eq
      value: true
